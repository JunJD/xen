[] 需要的是语法结构而非词法 ，语法会嵌套一般词汇，不需要改变原有大小，只能最多加不影响的类似边框效果，整体风格不能太乱
[] Queue/cache: add request queue + hash cache (dedupe, retry, fallback, persistence).
[] 回滚/还原：悬浮窗增加“标注/回顾(还原)”切换，需保留原始 DOM/HTML 以支持无损恢复。
